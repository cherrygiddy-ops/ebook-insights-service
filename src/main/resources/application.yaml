spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/ebookinsights
      database: ebookinsights
  jpa:
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8081
